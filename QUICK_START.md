# 快速开始指南

## 🚀 5分钟快速上手

### 第一步：安装依赖

```bash
npm install
```

### 第二步：启动项目

```bash
npm start
```

### 第三步：在手机上打开

#### 📱 iOS用户（iPhone/iPad）

1. 在 App Store 安装 **Expo Go**（免费，无需登录）
2. 运行 `npm start` 后，终端会显示二维码
3. 直接用 iPhone 的 **相机应用** 扫描二维码
4. 点击弹出的通知，应用会在 Expo Go 中打开

#### 🤖 Android用户

1. 在 Google Play 安装 **Expo Go**（免费，无需登录）
2. 运行 `npm start` 后，终端会显示二维码
3. 打开 Expo Go 应用
4. 点击 "Scan QR code" 扫描终端中的二维码
5. 应用会自动加载

### 💡 常见问题

#### Q: 二维码在哪里显示？
A: 运行 `npm start` 后，二维码会直接显示在终端窗口中。如果看不到，确保：
- 终端窗口足够宽
- 使用的是支持彩色显示的终端

#### Q: 需要登录吗？
A: **不需要！** Expo Go 可以直接扫描二维码使用，无需登录。登录是可选的，可以让你在不同设备间同步项目。

#### Q: 扫描后显示连接失败？
A: 确保：
- 手机和电脑连接在同一个Wi-Fi网络
- 防火墙没有阻止8081端口
- 尝试在终端按 `s` 切换到 Tunnel 模式（需要登录）

#### Q: 二维码太小看不清？
A: 在终端按以下快捷键：
- 按 `s` 显示二维码的URL链接，可以在浏览器打开
- 或者在手机浏览器输入终端显示的URL（exp://开头）

### 📝 开发提示

- **修改代码后自动刷新**: 保存文件后，手机上的应用会自动刷新
- **打开开发者菜单**: 
  - iOS: 摇动手机
  - Android: 按菜单键或三指下滑
- **重新加载**: 在开发者菜单中选择 "Reload"
- **查看错误**: 在开发者菜单中查看错误信息

### 🔧 其他运行方式

```bash
# 在iOS模拟器运行（需要Mac和Xcode）
npm run ios

# 在Android模拟器运行（需要Android Studio）
npm run android

# 在Web浏览器运行
npm run web
```

