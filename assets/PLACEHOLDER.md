# 资源文件占位符

这个目录需要以下图片文件才能完全消除警告：

- `icon.png` (1024x1024px) - 应用图标
- `splash.png` - 启动画面
- `adaptive-icon.png` (1024x1024px) - Android自适应图标
- `favicon.png` - Web图标

**注意**: 即使没有这些文件，应用仍然可以正常运行。这些警告不会影响开发。

要添加这些文件：
1. 准备符合尺寸要求的PNG图片
2. 将文件放在此目录中
3. 确保文件名与 `app.json` 中的配置一致

